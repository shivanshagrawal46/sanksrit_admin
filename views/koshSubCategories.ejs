<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kosh Subcategories | Sanskrit Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair+Display:wght@500;600;700&display=swap" rel="stylesheet">
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/sidebar.css" rel="stylesheet">
    <style>
        :root {
            --primary: #2D3250;
            --secondary: #424769;
            --accent: #676F9D;
            --light: #F9F9F9;
            --dark: #1A1B26;
            --success: #4CAF50;
            --error: #F44336;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--light);
            min-height: 100vh;
        }

        /* Main Content Styles */
        .main-content {
            margin-left: 280px;
            padding: 2rem;
        }

        .topbar {
            background: white;
            padding: 1rem 2rem;
            border-radius: 16px;
            box-shadow: var(--shadow);
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .page-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: var(--dark);
            margin: 0;
        }

        .user-menu {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: var(--primary);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
        }

        /* Card Styles */
        .content-card {
            background: white;
            border-radius: 16px;
            box-shadow: var(--shadow);
            margin-bottom: 2rem;
            overflow: hidden;
        }

        .card-header {
            background: var(--primary);
            color: white;
            padding: 1.25rem 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .card-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.25rem;
            margin: 0;
        }

        .card-body {
            padding: 2rem;
        }

        /* Button Styles */
        .btn-primary {
            background: var(--primary);
            border: none;
            border-radius: 12px;
            padding: 0.75rem 1.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            background: var(--secondary);
            transform: translateY(-1px);
            box-shadow: var(--shadow);
        }

        .btn-secondary {
            background: rgba(103, 111, 157, 0.1);
            color: var(--secondary);
            border: none;
            border-radius: 12px;
            padding: 0.75rem 1.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .btn-secondary:hover {
            background: rgba(103, 111, 157, 0.2);
            color: var(--primary);
        }

        .btn-danger {
            background: rgba(244, 67, 54, 0.1);
            color: var(--error);
            border: none;
            border-radius: 12px;
            padding: 0.75rem 1.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .btn-danger:hover {
            background: rgba(244, 67, 54, 0.2);
            color: var(--error);
        }

        /* Table Styles */
        .table {
            border-collapse: separate;
            border-spacing: 0 0.5rem;
            margin-top: 1rem;
            table-layout: fixed;
            width: 100%;
        }

        .table thead th {
            background: rgba(103, 111, 157, 0.1);
            padding: 1rem;
            color: var(--dark);
            font-weight: 600;
            border: none;
        }

        .table thead th:nth-child(1) { width: 50px; }  /* # */
        .table thead th:nth-child(2) { width: 150px; } /* Hindi */
        .table thead th:nth-child(3) { width: 120px; } /* English */
        .table thead th:nth-child(4) { width: 120px; } /* Hinglish */
        .table thead th:nth-child(5) { width: auto; }  /* Meaning - takes remaining space */
        .table thead th:nth-child(6) { width: 100px; min-width: 100px; } /* Actions - fixed width */

        .table tbody tr {
            background: white;
            transition: all 0.3s ease;
            margin-bottom: 0.5rem;
            box-shadow: var(--shadow);
            border-radius: 12px;
        }

        .table tbody tr:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }

        .table td {
            padding: 1rem;
            vertical-align: middle;
            border: none;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .table td:first-child {
            border-top-left-radius: 12px;
            border-bottom-left-radius: 12px;
        }

        .table td:last-child {
            border-top-right-radius: 12px;
            border-bottom-right-radius: 12px;
            white-space: nowrap;
        }

        .action-btns {
            display: flex;
            gap: 0.5rem;
            justify-content: center;
        }

        .btn-action {
            width: 36px;
            height: 36px;
            display: inline-flex !important;
            align-items: center;
            justify-content: center;
            border-radius: 8px;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }

        .btn-edit {
            background: rgba(103, 111, 157, 0.15) !important;
            color: var(--accent);
        }

        .btn-edit:hover {
            background: rgba(103, 111, 157, 0.3) !important;
            color: var(--primary);
            transform: scale(1.1);
        }

        .btn-delete {
            background: rgba(244, 67, 54, 0.15) !important;
            color: var(--error);
        }

        .btn-delete:hover {
            background: rgba(244, 67, 54, 0.3) !important;
            transform: scale(1.1);
        }

        /* Category Navigation */
        .category-nav {
            display: flex;
            overflow-x: auto;
            padding: 0.75rem 1.25rem;
            margin-bottom: 1.5rem;
            border-radius: 16px;
            background: #fff;
            box-shadow: var(--shadow);
            gap: 0.75rem;
            align-items: center;
        }

        .category-nav::-webkit-scrollbar {
            height: 4px;
        }

        .category-nav::-webkit-scrollbar-thumb {
            background: var(--accent);
            border-radius: 10px;
        }

        .category-nav-item {
            display: flex;
            align-items: center;
            white-space: nowrap;
            padding: 0.7rem 1.5rem;
            border-radius: 10px;
            font-weight: 500;
            color: var(--dark);
            background: transparent;
            transition: all 0.2s;
            text-decoration: none;
            font-size: 1.08rem;
            border: 1.5px solid transparent;
        }

        .category-nav-item:hover {
            background: rgba(103, 111, 157, 0.08);
            color: var(--primary);
            border-color: var(--accent);
            text-decoration: none;
        }

        .category-nav-item.active {
            background: var(--primary);
            color: #fff;
            border-color: var(--primary);
            font-weight: 600;
        }

        /* Subcategory Section */
        .subcategory-container {
            margin-top: 2rem;
        }

        .subcategory-item {
            background: white;
            border-radius: 12px;
            box-shadow: var(--shadow);
            margin-bottom: 1rem;
            padding: 1.5rem;
            transition: all 0.3s ease;
        }

        .subcategory-item:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }

        .subcategory-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1rem;
        }

        .subcategory-title {
            font-weight: 600;
            color: var(--dark);
            margin: 0;
        }

        .subcategory-badges {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .badge-position {
            background: rgba(103, 111, 157, 0.1);
            color: var(--primary);
            padding: 0.25rem 0.75rem;
            border-radius: 6px;
            font-size: 0.875rem;
        }

        .subcategory-content {
            color: var(--dark);
            opacity: 0.8;
            margin-bottom: 1rem;
        }

        .subcategory-footer {
            display: flex;
            justify-content: flex-end;
            gap: 0.75rem;
        }

        /* Pagination */
        .pagination {
            margin: 0;
            gap: 0.5rem;
        }

        .page-link {
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            color: var(--secondary);
            transition: all 0.3s ease;
        }

        .page-link:hover {
            background: rgba(103, 111, 157, 0.1);
            color: var(--primary);
        }

        .page-item.active .page-link {
            background: var(--primary);
            color: white;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <% if (subcategoryMode) { %>
        <nav class="sidebar">
            <%- include('partials/sidebar', { 
                activePage: 'kosh-subcategories', 
                activeCategory: null, 
                activeSubCategory: null,
                koshCategories: koshCategories
            }) %>
        </nav>
    <% } else if (parent) { %>
        <nav class="sidebar">
            <%- include('partials/sidebar', { 
                activePage: null, 
                activeCategory: parent ? parent._id.toString() : null, 
                activeSubCategory: selectedSubCategory ? selectedSubCategory._id.toString() : null,
                koshCategories: koshCategories
            }) %>
        </nav>
    <% } %>

    <!-- Main Content -->
    <main class="main-content">
        <!-- Topbar -->
            <div class="topbar">
            <h1 class="page-title">Kosh Subcategories</h1>
            <div class="user-menu">
                <div class="user-avatar">
                    <%= username ? username.charAt(0).toUpperCase() : 'A' %>
                </div>
            </div>
        </div>

        <% if (subcategoryMode) { %>
            <!-- CATEGORY NAVIGATION -->
            <div class="category-nav mb-4">
              <% koshCategories.forEach(cat => { %>
                <a href="/kosh-subcategories?category=<%= cat._id %>" 
                   class="category-nav-item <%= parent && parent._id.toString() === cat._id.toString() ? 'active' : '' %>">
                  <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="currentColor" style="vertical-align:middle;margin-right:0.5rem;opacity:0.7;" viewBox="0 0 16 16">
                    <path d="M2 2h12v12H2z" fill="none"/>
                    <path d="M2 2h12v12H2z" stroke="var(--accent)" stroke-width="1.5" fill="none"/>
                    <path d="M4 6h8M4 10h8" stroke="var(--primary)" stroke-width="1.2" stroke-linecap="round"/>
                  </svg>
                  <%= cat.name %>
                </a>
              <% }); %>
            </div>
            <div class="content-card">
                <div class="card-header">
                    <h2 class="card-title">Subcategories for <%= parent ? parent.name : '' %></h2>
                    <% if (parent) { %>
                    <a href="/kosh-category/<%= parent._id %>/add-subcategory" class="btn btn-primary">
                        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="currentColor" class="bi bi-plus-lg me-1" viewBox="0 0 16 16">
                            <path d="M8 0a1 1 0 0 1 1 1v6h6a1 1 0 1 1 0 2H9v6a1 1 0 1 1-2 0V9H1a1 1 0 0 1 0-2h6V1a1 1 0 0 1 1-1z"/>
                        </svg>
                        Add Subcategory
                    </a>
                    <% } %>
                </div>
                <div class="card-body">
                    <% if (!parent || subcategories.length === 0) { %>
                        <div class="text-center py-4">
                            <p>No subcategories found for this category.</p>
                            <% if (parent) { %>
                            <a href="/kosh-category/<%= parent._id %>/add-subcategory" class="btn btn-primary mt-2">Add First Subcategory</a>
                            <% } %>
                        </div>
                    <% } else { %>
                        <div class="table-responsive">
                            <table class="table">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Name</th>
                                        <th>Description</th>
                            <th>Position</th>
                            <th>Created At</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                                    <% subcategories.forEach((subcategory, idx) => { %>
                        <tr>
                            <td><%= idx + 1 %></td>
                                            <td><%= subcategory.name %></td>
                                            <td><%= subcategory.introduction || '' %></td>
                                            <td><%= subcategory.position %></td>
                                            <td><%= subcategory.createdAt ? new Date(subcategory.createdAt).toLocaleDateString() : '' %></td>
                            <td style="white-space: nowrap;">
                                                <a href="/kosh-subcategories?category=<%= parent._id %>&sub=<%= subcategory._id %>" class="btn btn-primary btn-sm me-1">Content</a>
                                                <a href="/kosh-subcategory/<%= subcategory._id %>/edit" class="btn btn-secondary btn-sm me-1">Edit</a>
                                                <form action="/kosh-subcategory/<%= subcategory._id %>/delete" method="POST" class="d-inline" data-subcategory-name="<%= subcategory.name %>" onsubmit="return confirmDeleteSubcategory(this)">
                                                    <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                                                </form>
                                            </td>
                                        </tr>
                                    <% }); %>
                                </tbody>
                            </table>
                        </div>
                    <% } %>
                </div>
            </div>
        <% } else if (parent) { %>
            <!-- SUBCATEGORY NAVIGATION & CONTENT TABLE -->
            <div class="category-nav mb-4">
                <% subcategories.forEach(sub => { %>
                    <a href="/kosh-subcategories/<%= parent._id %>?sub=<%= sub._id %>" class="category-nav-item <%= selectedSubCategory && selectedSubCategory._id.toString() === sub._id.toString() ? 'active' : '' %>">
                        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="currentColor" style="vertical-align:middle;margin-right:0.5rem;opacity:0.7;" viewBox="0 0 16 16">
                            <path d="M2 2h12v12H2z" fill="none"/>
                            <path d="M2 2h12v12H2z" stroke="var(--accent)" stroke-width="1.5" fill="none"/>
                            <path d="M4 6h8M4 10h8" stroke="var(--primary)" stroke-width="1.2" stroke-linecap="round"/>
                        </svg>
                        <%= sub.name %>
                    </a>
                <% }); %>
            </div>
            <% if (selectedSubCategory) { %>
            <div class="content-card">
                <div class="card-header">
                    <h2 class="card-title">Kosh Content for <%= parent.name %></h2>
                </div>
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <div>
                            <a href="/kosh-subcategory/<%= selectedSubCategory._id %>/add-content" class="btn btn-primary me-2">Add Content</a>
                            <a href="/kosh-subcategory/<%= selectedSubCategory._id %>/import-excel" class="btn btn-secondary me-2">Upload Excel</a>
                            <button type="button" class="btn btn-danger me-2" onclick="deleteAllContent()">Delete All</button>
                            <button type="button" class="btn btn-danger" onclick="deleteSelectedContent()" id="deleteSelectedBtn" style="display:none;">Delete Selected</button>
                        </div>
                        <div class="text-muted" style="font-size: 0.95rem;">
                            Showing <%= contents.length %> of <%= contentTotal %> items
                        </div>
                    </div>
                    <% if (contents.length === 0) { %>
                        <div class="text-center py-4">
                            <p>No content found for this subcategory.</p>
                        </div>
                    <% } else { %>
                    <form id="bulkDeleteForm" method="POST" action="/kosh-subcategory/<%= selectedSubCategory._id %>/delete-selected">
                        <div class="table-responsive" style="overflow-x: auto; max-width: 100%;">
                            <table class="table" style="min-width: 800px;">
                                <thead>
                                    <tr>
                                        <th style="width: 40px;">
                                            <input type="checkbox" id="selectAll" onchange="toggleSelectAll(this)">
                                        </th>
                                        <th>#</th>
                                        <th>Hindi</th>
                                        <th>English</th>
                                        <th>Hinglish</th>
                                        <th>Meaning</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% contents.forEach((content, idx) => { %>
                                        <tr>
                                            <td>
                                                <input type="checkbox" name="selectedContent" value="<%= content._id %>" class="content-checkbox" onchange="updateDeleteButton()">
                                            </td>
                                            <td><%= (currentPage - 1) * 10 + idx + 1 %></td>
                                            <td><%= content.hindiWord %></td>
                                            <td><%= content.englishWord %></td>
                                            <td><%= content.hinglishWord %></td>
                                            <td><%= content.meaning %></td>
                                            <td>
                                                <div class="action-btns">
                                                    <a href="/kosh-content/<%= content._id %>/edit" class="btn btn-action btn-edit" title="Edit">
                                                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
                                                            <path d="M12.146.146a.5.5 0 0 1 .708 0l3 3a.5.5 0 0 1 0 .708l-10 10a.5.5 0 0 1-.168.11l-5 2a.5.5 0 0 1-.65-.65l2-5a.5.5 0 0 1 .11-.168l10-10zM11.207 2.5 13.5 4.793 14.793 3.5 12.5 1.207 11.207 2.5zm1.586 3L10.5 3.207 4 9.707V10h.5a.5.5 0 0 1 .5.5v.5h.5a.5.5 0 0 1 .5.5v.5h.293l6.5-6.5zm-9.761 5.175-.106.106-1.528 3.821 3.821-1.528.106-.106A.5.5 0 0 1 5 12.5V12h-.5a.5.5 0 0 1-.5-.5V11h-.5a.5.5 0 0 1-.468-.325z"/>
                                                        </svg>
                                                    </a>
                                                    <form method="POST" action="/kosh-content/<%= content._id %>/delete" style="display:inline;margin:0;">
                                                        <button type="submit" class="btn btn-action btn-delete" title="Delete" onclick="return confirm('Delete this content?')">
                                                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
                                                                <path d="M5.5 5.5A.5.5 0 0 1 6 6v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5zm2.5 0a.5.5 0 0 1 .5.5v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5zm3 .5a.5.5 0 0 0-1 0v6a.5.5 0 0 0 1 0V6z"/>
                                                                <path fill-rule="evenodd" d="M14.5 3a1 1 0 0 1-1 1H13v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V4h-.5a1 1 0 0 1-1-1V2a1 1 0 0 1 1-1H6a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1h3.5a1 1 0 0 1 1 1v1zM4.118 4 4 4.059V13a1 1 0 0 0 1 1h6a1 1 0 0 0 1-1V4.059L11.882 4H4.118zM2.5 3V2h11v1h-11z"/>
                                                            </svg>
                                                        </button>
                                                    </form>
                                                </div>
                                            </td>
                                        </tr>
                                    <% }); %>
                                </tbody>
                            </table>
                        </div>
                    </form>
                    <% } %>
                    <% if (totalPages && totalPages > 1) { %>
                    <nav class="mt-4">
                        <ul class="pagination justify-content-center">
                            <% if (currentPage > 1) { %>
                                <li class="page-item">
                                    <a class="page-link" href="?sub=<%= selectedSubCategory._id %>&page=<%= currentPage - 1 %>">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
                                            <path fill-rule="evenodd" d="M11.354 1.646a.5.5 0 0 1 0 .708L5.707 8l5.647 5.646a.5.5 0 0 1-.708.708l-6-6a.5.5 0 0 1 0-.708l6-6a.5.5 0 0 1 .708 0z"/>
                                        </svg>
                                    </a>
                                </li>
                            <% } %>
                            <% for(let i = 1; i <= totalPages; i++) { %>
                                <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                                    <a class="page-link" href="?sub=<%= selectedSubCategory._id %>&page=<%= i %>"><%= i %></a>
                                </li>
                            <% } %>
                            <% if (currentPage < totalPages) { %>
                                <li class="page-item">
                                    <a class="page-link" href="?sub=<%= selectedSubCategory._id %>&page=<%= currentPage + 1 %>">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
                                            <path fill-rule="evenodd" d="M4.646 1.646a.5.5 0 0 1 .708 0l6 6a.5.5 0 0 1 0 .708l-6 6a.5.5 0 0 1-.708-.708L10.293 8 4.646 2.354a.5.5 0 0 1 0-.708z"/>
                                        </svg>
                                    </a>
                                </li>
                            <% } %>
                        </ul>
                    </nav>
                    <% } %>
                </div>
            </div>
            <% } else { %>
                <div class="content-card">
                    <div class="card-body text-center py-5">
                        <h3>Please select a subcategory from the navigation above</h3>
                        <p class="mt-3">Select a subcategory to view and manage its content.</p>
                    </div>
                </div>
            <% } %>
        <% } else { %>
            <div class="content-card">
                <div class="card-body text-center py-5">
                    <h3>Please select a Kosh category from the navigation above</h3>
                    <p class="mt-3">You can manage subcategories for each Kosh category.</p>
    </div>
</div>
        <% } %>
    </main>

    <script>
        // Only attach confirmation to subcategory delete forms
        const subcategoryDeleteForms = document.querySelectorAll('.delete-subcategory-form');
        subcategoryDeleteForms.forEach(form => {
            form.addEventListener('submit', function(e) {
                if (!confirm('Are you sure you want to delete this subcategory? This will also delete all content inside it!')) {
                    e.preventDefault();
                }
            });
        });

        // Toggle select all checkboxes
        function toggleSelectAll(checkbox) {
            const contentCheckboxes = document.querySelectorAll('.content-checkbox');
            contentCheckboxes.forEach(cb => {
                cb.checked = checkbox.checked;
            });
            updateDeleteButton();
        }

        // Update delete button visibility based on selection
        function updateDeleteButton() {
            const deleteBtn = document.getElementById('deleteSelectedBtn');
            const checkboxes = document.querySelectorAll('.content-checkbox:checked');
            if (checkboxes.length > 0) {
                deleteBtn.style.display = 'inline-block';
            } else {
                deleteBtn.style.display = 'none';
            }
        }

        // Delete all content
        function deleteAllContent() {
            if (confirm('⚠️ WARNING: This will permanently delete ALL content in this subcategory! Are you absolutely sure?')) {
                if (confirm('⚠️ FINAL WARNING: This action cannot be undone! Click OK to proceed with deleting ALL content.')) {
                    const subCategoryId = '<%= typeof selectedSubCategory !== "undefined" && selectedSubCategory ? selectedSubCategory._id : "" %>';
                    if (!subCategoryId) {
                        alert('Error: No subcategory selected');
                        return;
                    }
                    const form = document.createElement('form');
                    form.method = 'POST';
                    form.action = '/kosh-subcategory/' + subCategoryId + '/delete-all-content';
                    document.body.appendChild(form);
                    form.submit();
                }
            }
        }

        // Delete selected content
        function deleteSelectedContent() {
            const checkboxes = document.querySelectorAll('.content-checkbox:checked');
            if (checkboxes.length === 0) {
                alert('Please select at least one item to delete.');
                return;
            }
            if (confirm(`Are you sure you want to delete ${checkboxes.length} selected item(s)?`)) {
                document.getElementById('bulkDeleteForm').submit();
            }
        }

        // Confirm delete subcategory
        function confirmDeleteSubcategory(form) {
            const subcategoryName = form.getAttribute('data-subcategory-name');
            return confirm(`⚠️ WARNING: Are you sure you want to delete the subcategory "${subcategoryName}"?\n\nThis will also permanently delete ALL content inside this subcategory!\n\nThis action cannot be undone.`);
        }
    </script>
</body>
</html> 