<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MCQ Masters | Sanskrit Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair+Display:wght@500;600;700&display=swap" rel="stylesheet">
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/sidebar.css" rel="stylesheet">
    <style>
        :root {
            --primary: #2D3250;
            --secondary: #424769;
            --accent: #676F9D;
            --light: #F9F9F9;
            --dark: #1A1B26;
            --success: #4CAF50;
            --error: #F44336;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }
        body {
            font-family: 'Inter', sans-serif;
            background: var(--light);
            min-height: 100vh;
        }
        .main-content {
            margin-left: 280px;
            padding: 2rem;
        }
        .topbar {
            background: white;
            padding: 1rem 2rem;
            border-radius: 16px;
            box-shadow: var(--shadow);
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        .page-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: var(--dark);
            margin: 0;
        }
        .user-menu {
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: var(--primary);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
        }
        .category-nav {
            display: flex;
            overflow-x: auto;
            padding: 0.75rem 1.25rem;
            margin-bottom: 1.5rem;
            border-radius: 16px;
            background: #fff;
            box-shadow: var(--shadow);
            gap: 0.75rem;
            align-items: center;
        }
        .category-nav-item {
            display: flex;
            align-items: center;
            white-space: nowrap;
            padding: 0.7rem 1.5rem;
            border-radius: 10px;
            font-weight: 500;
            color: var(--dark);
            background: transparent;
            transition: all 0.2s;
            text-decoration: none;
        }
        .category-nav-item.active, .category-nav-item:hover {
            background: var(--primary);
            color: #fff;
        }
        .category-nav-item svg {
            margin-right: 0.5rem;
            opacity: 0.7;
        }
        .content-card {
            background: white;
            border-radius: 16px;
            box-shadow: var(--shadow);
            margin-bottom: 2rem;
            overflow: hidden;
        }
        .card-header {
            background: var(--primary);
            color: white;
            padding: 1.25rem 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        .card-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.25rem;
            margin: 0;
        }
        .card-body {
            padding: 2rem;
        }
        .btn-primary {
            background: var(--primary);
            border: none;
            border-radius: 12px;
            padding: 0.75rem 1.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .btn-primary:hover {
            background: var(--secondary);
            transform: translateY(-1px);
            box-shadow: var(--shadow);
        }
        .btn-secondary {
            background: rgba(103, 111, 157, 0.1);
            color: var(--secondary);
            border: none;
            border-radius: 12px;
            padding: 0.75rem 1.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .btn-secondary:hover {
            background: rgba(103, 111, 157, 0.2);
            color: var(--primary);
        }
        .btn-danger {
            background: rgba(244, 67, 54, 0.1);
            color: var(--error);
            border: none;
            border-radius: 12px;
            padding: 0.75rem 1.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .btn-danger:hover {
            background: rgba(244, 67, 54, 0.2);
            color: var(--error);
        }
        .table {
            border-collapse: separate;
            border-spacing: 0 0.5rem;
            margin-top: 1rem;
        }
        .table thead th {
            background: rgba(103, 111, 157, 0.1);
            padding: 1rem;
            color: var(--dark);
            font-weight: 600;
            border: none;
        }
        .table tbody tr {
            background: white;
            transition: all 0.3s ease;
            margin-bottom: 0.5rem;
            box-shadow: var(--shadow);
            border-radius: 12px;
        }
        .table tbody tr:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }
        .table td {
            padding: 1rem;
            vertical-align: middle;
            border: none;
        }
        .table td:first-child {
            border-top-left-radius: 12px;
            border-bottom-left-radius: 12px;
        }
        .table td:last-child {
            border-top-right-radius: 12px;
            border-bottom-right-radius: 12px;
        }
        .action-btns {
            display: flex;
            gap: 0.5rem;
        }
        .btn-action {
            width: 36px;
            height: 36px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 8px;
        }
        .btn-edit {
            background: rgba(103, 111, 157, 0.1);
            color: var(--primary);
            border: none;
            border-radius: 10px;
            padding: 0.4rem 1rem;
            font-size: 0.98rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .btn-edit:hover {
            background: var(--primary);
            color: #fff;
        }
        .btn-delete {
            background: rgba(244, 67, 54, 0.1);
            color: var(--error);
            border: none;
            border-radius: 10px;
            padding: 0.4rem 1rem;
            font-size: 0.98rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .btn-delete:hover {
            background: var(--error);
            color: #fff;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <% if (masterMode) { %>
        <nav class="sidebar">
            <%- include('partials/sidebar', {
                activePage: 'mcq-master',
                activeCategory: null,
                activeSubCategory: null,
                koshCategories: koshCategories,
                username: username
            }) %>
        </nav>
    <% } else if (parent) { %>
        <nav class="sidebar">
            <%- include('partials/sidebar', {
                activePage: null,
                activeCategory: parent._id.toString(),
                activeSubCategory: null,
                koshCategories: koshCategories,
                username: username
            }) %>
        </nav>
    <% } %>

    <main class="main-content">
        <div class="topbar">
            <h1 class="page-title">MCQ Masters</h1>
            <div class="user-menu">
                <div class="user-avatar">
                    <%= username ? username.charAt(0).toUpperCase() : 'A' %>
                </div>
            </div>
        </div>

        <% if (masterMode) { %>
            <!-- CATEGORY NAVIGATION -->
            <div class="category-nav mb-4">
                <% mcqCategories.forEach(cat => { %>
                    <a href="/mcq-master?category=<%= cat._id %>"
                       class="category-nav-item <%= selectedCategory && selectedCategory._id.toString() === cat._id.toString() ? 'active' : '' %>">
                       <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="currentColor" style="vertical-align:middle;margin-right:0.5rem;opacity:0.7;" viewBox="0 0 16 16">
                        <path d="M2 2h12v12H2z" fill="none"/>
                        <path d="M2 2h12v12H2z" stroke="var(--accent)" stroke-width="1.5" fill="none"/>
                        <path d="M4 6h8M4 10h8" stroke="var(--primary)" stroke-width="1.2" stroke-linecap="round"/>
                      </svg>
                        <%= cat.name %>
                    </a>
                <% }); %>
            </div>

            <!-- Masters Table -->
            <div class="content-card">
                <div class="card-header">
                    <span class="card-title">MCQ Masters for <%= selectedCategory ? selectedCategory.name : 'All Categories' %></span>
                    <% if (selectedCategory) { %>
                        <a href="/mcq-category/<%= selectedCategory._id %>/add-master" class="btn btn-light btn-sm" style="background:#fff;color:var(--primary);border-radius:8px;font-weight:600;">
                            + Add MCQ Master
                        </a>
                    <% } %>
                </div>
                <div class="card-body">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Position</th>
                                <th>Introduction</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (masters && masters.length > 0) { %>
                                <% masters.forEach(function(master) { %>
                                    <tr>
                                        <td><%= master.name %></td>
                                        <td><%= master.position %></td>
                                        <td><%= master.introduction %></td>
                                        <td class="action-btns">
                                            <div class="d-flex gap-2">
                                                <a href="/mcq-master/<%= master._id %>/edit" class="btn btn-edit">Edit</a>
                                                <form method="POST" action="/mcq-master/<%= master._id %>/delete" style="display:inline; margin:0;">
                                                    <button type="submit" class="btn btn-delete" onclick="return confirm('Delete this master?')">Delete</button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } else { %>
                                <tr><td colspan="4" class="text-center">No MCQ masters found.</td></tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        <% } else if (parent) { %>
            <!-- MCQ MASTERS NAVIGATION BAR -->
            <div class="category-nav mb-4">
                <% masters.forEach(function(master) { %>
                    <a href="/mcq-category/<%= parent._id %>/masters?master=<%= master._id %>"
                       class="category-nav-item <%= (typeof selectedMaster !== 'undefined' && selectedMaster && selectedMaster._id.toString() === master._id.toString()) ? 'active' : '' %>">
                        <%= master.name %>
                    </a>
                <% }) %>
            </div>
            <div class="content-card">
                <div class="card-body">
                    <% if (typeof selectedMaster !== 'undefined' && selectedMaster) { %>
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <h3 class="mb-0">MCQ Content for <%= selectedMaster.name %></h3>
                            <a href="/mcq-content/master/<%= selectedMaster._id %>/add" class="btn btn-success">Add Content</a>
                        </div>
                        <% if (contents && contents.length > 0) { %>
                            <div class="table-responsive">
                                <table class="table table-bordered table-hover">
                                    <thead>
                                        <tr>
                                            <th>#</th>
                                            <th>Question</th>
                                            <th>Options</th>
                                            <th>Correct</th>
                                            <th>Explanation</th>
                                            <th>References</th>
                                            <th>Image</th>
                                            <th>Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% contents.forEach(function(content, idx) { %>
                                        <tr>
                                            <td><%= idx + 1 %></td>
                                            <td><%= content.question %></td>
                                            <td>
                                                <ol>
                                                    <% content.options.forEach(function(option) { %>
                                                        <li><%= option %></li>
                                                    <% }) %>
                                                </ol>
                                            </td>
                                            <td><%= content.options[content.correctAnswer] %></td>
                                            <td><%= content.explanation %></td>
                                            <td>
                                                <% if (content.references && content.references.length > 0) { %>
                                                    <ul>
                                                        <% content.references.forEach(function(ref) { %>
                                                            <li><%= ref %></li>
                                                        <% }) %>
                                                    </ul>
                                                <% } %>
                                            </td>
                                            <td>
                                                <% if (content.image) { %>
                                                    <img src="<%= content.image %>" style="max-width: 60px;">
                                                <% } %>
                                            </td>
                                            <td>
                                                <a href="/mcq-content/<%= content._id %>/edit" class="btn btn-primary btn-sm">Edit</a>
                                                <form method="POST" action="/mcq-content/<%= content._id %>/delete" style="display:inline;">
                                                    <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Delete this MCQ?')">Delete</button>
                                                </form>
                                            </td>
                                        </tr>
                                        <% }) %>
                                    </tbody>
                                </table>
                            </div>
                        <% } else { %>
                            <div class="alert alert-info">No MCQ content found for this master.</div>
                        <% } %>
                    <% } else { %>
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <a href="/mcq-category/<%= parent._id %>/add-master" class="btn btn-primary">+ Add MCQ Master</a>
                        </div>
                        <div class="alert alert-warning">Please select an MCQ Master above to view or add content.</div>
                    <% } %>
                </div>
            </div>
        <% } %>
    </main>
</body>
</html> 