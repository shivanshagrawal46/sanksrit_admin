<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= chapter.name %> - Content | Sanskrit Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair+Display:wght@500;600;700&display=swap" rel="stylesheet">
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/sidebar.css" rel="stylesheet">
    <style>
        :root {
            --primary: #2D3250;
            --secondary: #424769;
            --accent: #676F9D;
            --light: #F9F9F9;
            --dark: #1A1B26;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }
        body {
            font-family: 'Inter', sans-serif;
            background: var(--light);
            min-height: 100vh;
        }
        .main-content {
            margin-left: 280px;
            padding: 2rem;
        }
        .topbar {
            background: white;
            padding: 1rem 2rem;
            border-radius: 16px;
            box-shadow: var(--shadow);
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        .page-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: var(--dark);
            margin: 0;
        }
        .breadcrumb-nav {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 1rem;
            color: var(--secondary);
            font-size: 0.9rem;
        }
        .breadcrumb-nav a {
            color: var(--primary);
            text-decoration: none;
        }
        .breadcrumb-nav a:hover {
            text-decoration: underline;
        }
        .user-menu {
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: var(--primary);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
        }
        .btn-primary {
            background: var(--primary);
            border: none;
            border-radius: 12px;
            padding: 0.75rem 1.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
            color: white;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        .btn-primary:hover {
            background: var(--secondary);
            color: white;
            transform: translateY(-1px);
        }
        .btn-secondary {
            background: rgba(103, 111, 157, 0.1);
            color: var(--secondary);
            border: none;
            border-radius: 12px;
            padding: 0.75rem 1.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        .btn-secondary:hover {
            background: rgba(103, 111, 157, 0.2);
            color: var(--primary);
        }
        .content-card {
            background: white;
            border-radius: 16px;
            box-shadow: var(--shadow);
            padding: 2rem;
            margin-bottom: 1.5rem;
        }
        .content-title {
            font-size: 1.2rem;
            font-weight: 600;
            color: var(--dark);
            margin-bottom: 0.5rem;
        }
        .content-title a {
            color: var(--primary);
            text-decoration: none;
        }
        .content-title a:hover {
            text-decoration: underline;
        }
        .content-details {
            color: var(--secondary);
            margin-top: 0.5rem;
        }
        .content-images {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
            margin-top: 1rem;
        }
        .content-image {
            max-width: 120px;
            max-height: 120px;
            border-radius: 8px;
            object-fit: cover;
        }
        .empty-state {
            text-align: center;
            padding: 3rem 2rem;
            color: var(--secondary);
        }
        .empty-state svg {
            width: 64px;
            height: 64px;
            margin-bottom: 1rem;
            opacity: 0.5;
        }
        .action-buttons {
            display: flex;
            gap: 0.75rem;
            align-items: center;
        }
    </style>
</head>
<body>
    <nav class="sidebar">
        <%- include('../../partials/sidebar', { 
            activePage: 'book',
            activeCategory: null,
            koshCategories: [],
            mcqCategories: []
        }) %>
    </nav>
    <main class="main-content">
        <!-- Breadcrumb -->
        <div class="breadcrumb-nav">
            <a href="/book">Book Categories</a>
            <span>/</span>
            <a href="/book/category/<%= category._id %>"><%= category.name %></a>
            <span>/</span>
            <a href="/book/category/<%= category._id %>/book/<%= book._id %>"><%= book.name %></a>
            <span>/</span>
            <span><%= chapter.name %></span>
        </div>

        <!-- Topbar -->
        <div class="topbar">
            <div>
                <h1 class="page-title"><%= chapter.name %></h1>
                <div class="text-muted">Book: <%= book.name %> | Category: <%= category.name %></div>
            </div>
            <div class="d-flex align-items-center gap-3">
                <div class="action-buttons">
                    <a href="/book/content/add?category=<%= category._id %>&book=<%= book._id %>&chapter=<%= chapter._id %>" class="btn btn-primary">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
                        </svg>
                        Add Content
                    </a>
                    <button type="button" class="btn btn-secondary" data-bs-toggle="modal" data-bs-target="#uploadExcelModal">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12" />
                        </svg>
                        Upload Excel
                    </button>
                    <a href="/book/content/export-excel" class="btn btn-secondary" style="background:#16a34a;color:white;">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                        </svg>
                        Download Template
                    </a>
                </div>
                <div class="user-menu">
                    <div class="user-avatar">
                        <%= username ? username.charAt(0).toUpperCase() : 'A' %>
                    </div>
                </div>
            </div>
        </div>

        <!-- Success/Error Messages -->
        <script>
            const urlParams = new URLSearchParams(window.location.search);
            const success = urlParams.get('success');
            const error = urlParams.get('error');
            if (success) {
                const alertDiv = document.createElement('div');
                alertDiv.className = 'alert alert-success alert-dismissible fade show';
                alertDiv.style.marginBottom = '2rem';
                alertDiv.innerHTML = decodeURIComponent(success) + '<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>';
                document.querySelector('.main-content').insertBefore(alertDiv, document.querySelector('.breadcrumb-nav').nextSibling);
            }
            if (error) {
                const alertDiv = document.createElement('div');
                alertDiv.className = 'alert alert-danger alert-dismissible fade show';
                alertDiv.style.marginBottom = '2rem';
                alertDiv.innerHTML = decodeURIComponent(error) + '<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>';
                document.querySelector('.main-content').insertBefore(alertDiv, document.querySelector('.breadcrumb-nav').nextSibling);
            }
        </script>

        <!-- Content List -->
        <% if (contents && contents.length > 0) { %>
            <% contents.forEach(function(content) { %>
                <div class="content-card">
                    <div class="content-title">
                        <a href="/book/content/edit/<%= content._id %>">
                            <%= content.title_hn || content.title_en || content.title_hinglish || 'Untitled' %>
                        </a>
                    </div>
                    <div class="content-details">
                        <%= content.details ? content.details : '<span class="text-muted">No details available.</span>' %>
                    </div>
                    <% if (content.images && content.images.length > 0) { %>
                        <div class="content-images">
                            <% content.images.forEach(function(img) { %>
                                <img src="<%= img %>" alt="Content image" class="content-image">
                            <% }); %>
                        </div>
                    <% } %>
                    <% if (content.video_links && content.video_links.length > 0) { %>
                        <div class="mt-3">
                            <strong>Videos:</strong>
                            <ul>
                                <% content.video_links.forEach(function(link) { %>
                                    <li><a href="<%= link %>" target="_blank"><%= link %></a></li>
                                <% }); %>
                            </ul>
                        </div>
                    <% } %>
                    <div class="mt-3">
                        <a href="/book/content/edit/<%= content._id %>" class="btn btn-sm btn-outline-primary me-2">Edit</a>
                        <form action="/book/content/delete/<%= content._id %>" method="POST" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this content?');">
                            <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                        </form>
                    </div>
                </div>
            <% }); %>
        <% } else { %>
            <div class="content-card">
                <div class="empty-state">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                    </svg>
                    <h3>No Content Found</h3>
                    <p>Get started by adding content to this chapter.</p>
                    <div class="d-flex justify-content-center gap-2 mt-3">
                        <a href="/book/content/add?category=<%= category._id %>&book=<%= book._id %>&chapter=<%= chapter._id %>" class="btn btn-primary">
                            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
                            </svg>
                            Add Content
                        </a>
                        <button type="button" class="btn btn-secondary" data-bs-toggle="modal" data-bs-target="#uploadExcelModal">
                            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12" />
                            </svg>
                            Upload Excel
                        </button>
                    </div>
                </div>
            </div>
        <% } %>
    </main>

    <!-- Upload Excel Modal -->
    <div class="modal fade" id="uploadExcelModal" tabindex="-1" aria-labelledby="uploadExcelModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="uploadExcelModalLabel">Upload Book Content Excel</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form action="/book/content/upload-excel" method="POST" enctype="multipart/form-data">
                        <input type="hidden" name="category" value="<%= category._id %>">
                        <input type="hidden" name="book" value="<%= book._id %>">
                        <input type="hidden" name="chapter" value="<%= chapter._id %>">
                        <div class="mb-3">
                            <label for="excelFile" class="form-label">Select Excel File</label>
                            <input type="file" class="form-control" id="excelFile" name="excelFile" accept=".xlsx,.xls" required>
                        </div>
                        <div class="alert alert-info">
                            <small>
                                <strong>📝 Upload Instructions:</strong>
                                <div class="mt-2 mb-2">
                                    Content will be added to: <strong><%= category.name %> → <%= book.name %> → <%= chapter.name %></strong>
                                </div>
                                <div class="mb-2">
                                    <strong>Excel Format:</strong> Your Excel file should have these columns (all fields are optional):
                                </div>
                                <div style="background: #f8f9fa; padding: 0.5rem; border-radius: 4px; font-family: monospace; font-size: 0.85em;">
                                    Title (Hindi) | Title (English) | Title (Hinglish) | Meaning | Details | Extra | Video Links
                                </div>
                                <div class="mt-3 text-center">
                                    <a href="/book/content/export-excel" class="btn btn-sm btn-outline-primary">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
                                            <path d="M.5 9.9a.5.5 0 0 1 .5.5v2.5a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1v-2.5a.5.5 0 0 1 1 0v2.5a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2v-2.5a.5.5 0 0 1 .5-.5z"/>
                                            <path d="M7.646 11.854a.5.5 0 0 0 .708 0l3-3a.5.5 0 0 0-.708-.708L8.5 10.293V1.5a.5.5 0 0 0-1 0v8.793L5.354 8.146a.5.5 0 1 0-.708.708l3 3z"/>
                                        </svg>
                                        Download Excel Template
                                    </a>
                                </div>
                            </small>
                        </div>
                        <div class="text-end">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                            <button type="submit" class="btn btn-primary">Upload</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
</body>
</html>
