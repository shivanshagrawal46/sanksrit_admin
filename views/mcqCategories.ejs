<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MCQ Categories | Sanskrit Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair+Display:wght@500;600;700&display=swap" rel="stylesheet">
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/sidebar.css" rel="stylesheet">
    <style>
        :root {
            --primary: #2D3250;
            --secondary: #424769;
            --accent: #676F9D;
            --light: #F9F9F9;
            --dark: #1A1B26;
            --success: #4CAF50;
            --error: #F44336;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--light);
            min-height: 100vh;
        }

        /* Sidebar Styles */
        .sidebar {
            background: white;
            min-height: 100vh;
            border-right: 1px solid rgba(0, 0, 0, 0.05);
            padding: 2rem 0;
            position: fixed;
            width: 280px;
            top: 0;
            left: 0;
            z-index: 1000;
            transition: all 0.3s ease;
        }

        .sidebar-header {
            padding: 0 2rem;
            margin-bottom: 2rem;
        }

        .logo {
            width: 120px;
            height: 120px;
            margin-bottom: 1rem;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid var(--accent);
            filter: drop-shadow(0 4px 6px rgba(0, 0, 0, 0.1));
        }

        .nav-item {
            margin: 0.5rem 0;
        }

        .nav-link {
            color: var(--dark);
            padding: 0.75rem 2rem;
            border-radius: 12px;
            margin: 0 1rem;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            font-weight: 500;
        }

        .nav-link:hover {
            background: rgba(103, 111, 157, 0.1);
            color: var(--primary);
        }

        .nav-link.active {
            background: var(--primary);
            color: white;
        }

        .nav-link svg {
            margin-right: 0.75rem;
            width: 20px;
            height: 20px;
        }

        /* Main Content Styles */
        .main-content {
            margin-left: 280px;
            padding: 2rem;
        }

        .topbar {
            background: white;
            padding: 1rem 2rem;
            border-radius: 16px;
            box-shadow: var(--shadow);
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .page-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: var(--dark);
            margin: 0;
        }

        .user-menu {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: var(--primary);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
        }

        /* Content Card */
        .content-card {
            background: white;
            border-radius: 16px;
            box-shadow: var(--shadow);
            padding: 1.5rem;
            margin-bottom: 2rem;
        }

        .card-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 1.5rem;
        }

        .btn-add {
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 12px;
            padding: 0.75rem 1.5rem;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.3s ease;
        }

        .btn-add:hover {
            background: var(--secondary);
            transform: translateY(-1px);
            box-shadow: var(--shadow);
        }

        /* Table Styles */
        .table {
            margin: 0;
        }

        .table th {
            font-weight: 600;
            color: var(--dark);
            border-bottom: 2px solid rgba(0, 0, 0, 0.05);
            padding: 1rem;
        }

        .table td {
            padding: 1rem;
            vertical-align: middle;
            color: var(--secondary);
        }

        .table tr {
            transition: all 0.3s ease;
        }

        .table tr:hover {
            background: rgba(103, 111, 157, 0.05);
        }

        .btn-action {
            padding: 0.5rem;
            border-radius: 8px;
            transition: all 0.3s ease;
            color: var(--secondary);
        }

        .btn-action:hover {
            background: rgba(103, 111, 157, 0.1);
            color: var(--primary);
        }

        .btn-action.delete:hover {
            background: rgba(244, 67, 54, 0.1);
            color: var(--error);
        }
        
        .btn-edit, .btn-delete {
            padding: 0.5rem 1rem;
            border-radius: 8px;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        
        .btn-edit {
            background: rgba(103, 111, 157, 0.1);
            color: var(--primary);
            border: none;
        }
        
        .btn-edit:hover {
            background: var(--primary);
            color: white;
        }
        
        .btn-delete {
            background: rgba(244, 67, 54, 0.1);
            color: var(--error);
            border: none;
        }
        
        .btn-delete:hover {
            background: var(--error);
            color: white;
        }

        .sidebar-content {
            flex: 1 1 auto;
            overflow-y: auto;
            max-height: calc(100vh - 120px);
            padding-bottom: 1rem;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <nav class="sidebar">
        <%- include('partials/sidebar', {
            activePage: 'mcq-categories',
            koshCategories: typeof koshCategories !== 'undefined' ? koshCategories : [],
            mcqCategories: typeof mcqCategories !== 'undefined' ? mcqCategories : categories,
            username: username,
            activeCategory: null,
            activeSubCategory: null
        }) %>
        </nav>

    <!-- Main Content -->
    <main class="main-content">
        <!-- Topbar -->
            <div class="topbar">
            <h1 class="page-title">MCQ Categories</h1>
            <div class="user-menu">
                <div class="user-avatar">
                    <%= username ? username.charAt(0).toUpperCase() : 'A' %>
                </div>
            </div>
        </div>

        <!-- MCQ Categories -->
        <div class="content-card">
            <div class="card-header">
                <h2 class="card-title">All MCQ Categories</h2>
                <a href="/add-mcq-category" class="btn btn-add">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
                        <path d="M8 4a.5.5 0 0 1 .5.5v3h3a.5.5 0 0 1 0 1h-3v3a.5.5 0 0 1-1 0v-3h-3a.5.5 0 0 1 0-1h3v-3A.5.5 0 0 1 8 4z"/>
                    </svg>
                    Add New Category
                </a>
            </div>
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Name</th>
                            <th>Position</th>
                            <th>Introduction</th>
                            <th>Created At</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% categories.forEach(function(cat, idx) { %>
                        <tr>
                            <td><%= idx + 1 %></td>
                            <td><%= cat.name %></td>
                            <td><%= cat.position %></td>
                            <td><%= cat.introduction %></td>
                            <td><%= cat.createdAt.toLocaleString() %></td>
                            <td>
                                <div class="d-flex gap-2">
                                    <a href="/edit-mcq-category/<%= cat._id %>" class="btn btn-edit">Edit</a>
                                <form method="POST" action="/delete-mcq-category/<%= cat._id %>" style="display:inline;">
                                        <button type="submit" class="btn btn-delete" onclick="return confirm('Are you sure you want to delete this category?')">Delete</button>
                                </form>
                                </div>
                            </td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
            </div>
        </main>
</body>
</html> 