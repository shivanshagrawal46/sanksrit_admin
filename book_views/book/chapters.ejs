<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= book.name %> - Chapters | Sanskrit Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair+Display:wght@500;600;700&display=swap" rel="stylesheet">
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/sidebar.css" rel="stylesheet">
    <style>
        :root {
            --primary: #2D3250;
            --secondary: #424769;
            --accent: #676F9D;
            --light: #F9F9F9;
            --dark: #1A1B26;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }
        body {
            font-family: 'Inter', sans-serif;
            background: var(--light);
            min-height: 100vh;
        }
        .main-content {
            margin-left: 280px;
            padding: 2rem;
        }
        .topbar {
            background: white;
            padding: 1rem 2rem;
            border-radius: 16px;
            box-shadow: var(--shadow);
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        .page-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: var(--dark);
            margin: 0;
        }
        .breadcrumb-nav {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 1rem;
            color: var(--secondary);
            font-size: 0.9rem;
        }
        .breadcrumb-nav a {
            color: var(--primary);
            text-decoration: none;
        }
        .breadcrumb-nav a:hover {
            text-decoration: underline;
        }
        .user-menu {
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: var(--primary);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
        }
        .btn-primary {
            background: var(--primary);
            border: none;
            border-radius: 12px;
            padding: 0.75rem 1.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
            color: white;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        .btn-primary:hover {
            background: var(--secondary);
            color: white;
            transform: translateY(-1px);
        }
        .content-card {
            background: white;
            border-radius: 16px;
            box-shadow: var(--shadow);
            overflow: hidden;
        }
        .card-header {
            background: var(--primary);
            color: white;
            padding: 1.25rem 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        .card-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.25rem;
            margin: 0;
        }
        .card-body {
            padding: 2rem;
        }
        .chapter-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        .chapter-item {
            background: white;
            border-radius: 12px;
            box-shadow: var(--shadow);
            padding: 1.25rem 1.5rem;
            margin-bottom: 1rem;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        .chapter-item:hover {
            transform: translateX(4px);
            box-shadow: 0 6px 12px -2px rgba(0, 0, 0, 0.1);
        }
        .chapter-link {
            text-decoration: none;
            color: var(--dark);
            font-weight: 500;
            font-size: 1.1rem;
            flex: 1;
        }
        .chapter-link:hover {
            color: var(--primary);
            text-decoration: none;
        }
        .chapter-actions {
            display: flex;
            gap: 0.5rem;
        }
        .btn-sm {
            padding: 0.4rem 0.8rem;
            font-size: 0.875rem;
            border-radius: 8px;
        }
        .empty-state {
            text-align: center;
            padding: 3rem 2rem;
            color: var(--secondary);
        }
        .empty-state svg {
            width: 64px;
            height: 64px;
            margin-bottom: 1rem;
            opacity: 0.5;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <nav class="sidebar">
        <%- include('../../partials/sidebar', { 
            activePage: 'book',
            activeCategory: null,
            koshCategories: [],
            mcqCategories: []
        }) %>
    </nav>

    <!-- Main Content -->
    <main class="main-content">
        <!-- Breadcrumb -->
        <div class="breadcrumb-nav">
            <a href="/book">Book Categories</a>
            <span>/</span>
            <a href="/book/category/<%= category._id %>"><%= category.name %></a>
            <span>/</span>
            <span><%= book.name %></span>
        </div>

        <!-- Topbar -->
        <div class="topbar">
            <div>
                <h1 class="page-title"><%= book.name %></h1>
                <div class="text-muted">Chapters in this book</div>
            </div>
            <div class="d-flex align-items-center gap-3">
                <a href="/book/chapter/add?book=<%= book._id %>&category=<%= category._id %>" class="btn btn-primary">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
                    </svg>
                    Add Chapter
                </a>
                <div class="user-menu">
                    <div class="user-avatar">
                        <%= username ? username.charAt(0).toUpperCase() : 'A' %>
                    </div>
                </div>
            </div>
        </div>

        <!-- Chapters List -->
        <div class="content-card">
            <div class="card-body">
                <% if (chapters && chapters.length > 0) { %>
                    <ul class="chapter-list">
                        <% chapters.forEach(function(chapter) { %>
                            <li class="chapter-item">
                                <a href="/book/category/<%= category._id %>/book/<%= book._id %>/chapter/<%= chapter._id %>" class="chapter-link">
                                    <%= chapter.name %>
                                </a>
                                <div class="chapter-actions">
                                    <a href="/book/chapter/edit/<%= chapter._id %>?return=/book/category/<%= category._id %>/book/<%= book._id %>" class="btn btn-sm btn-outline-primary">Edit</a>
                                    <form action="/book/chapter/delete/<%= chapter._id %>" method="POST" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this chapter? This will also delete all content in this chapter.');">
                                        <input type="hidden" name="return" value="/book/category/<%= category._id %>/book/<%= book._id %>">
                                        <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                                    </form>
                                </div>
                            </li>
                        <% }); %>
                    </ul>
                <% } else { %>
                    <div class="empty-state">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                        </svg>
                        <h3>No Chapters Found</h3>
                        <p>Get started by adding your first chapter to this book.</p>
                        <a href="/book/chapter/add?book=<%= book._id %>&category=<%= category._id %>" class="btn btn-primary mt-3">
                            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
                            </svg>
                            Add Chapter
                        </a>
                    </div>
                <% } %>
            </div>
        </div>
    </main>

    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
</body>
</html>

